spring:
  application:
    name: ai-service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}
    prefer-ip-address: true
dynamodb:
  region: ${AWS_REGION}

jwt:
  secret: ${JWT_SECRET}

server:
  port: 8111